# HubSpot Private App Token (required)
HUBSPOT_TOKEN=your-private-app-token-here

# App password — leave blank for no auth, set to protect the dashboard
APP_PASSWORD=

# Port (default 3000)
PORT=3000

# Data directory — override for Railway persistent volume
# DATA_DIR=/app/data
